services:
  fuelstopbot:
    image: ${DOCKER_REGISTRY:-fuelstopbot}
    build:
      context: .
      dockerfile: src/Dockerfile
    env_file:
      - .env
    environment:
       - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}