services:
  fuelstopbot:
    image: ${DOCKER_REGISTRY:-fuelstopbot}
    build:
      context: .
      dockerfile: src/Dockerfile
    env_file:
      - secrets.env
    environment:
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}